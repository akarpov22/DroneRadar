type Mutation {
    appendPosition(input: AppendPositionInput!): Position!
}

input AppendPositionInput {
  droneSerial: String!
  latitude: Float!
  longitude: Float!
  altitude: Float!
  heading: Float
  speed: Float
  timestamp: DateTime!
}

